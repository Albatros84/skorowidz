 To pochodzi z partiala, a reszta strony działa jak należy
 

<%= select_tag "user", options_from_collection_for_select(@users, "id", "name")%>

<%= select_tag "role", options_for_select([ "trener", "asystent", "koordynator merytoryczny", "reporter" ]) %>